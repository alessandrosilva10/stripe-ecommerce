import styled from "styled-components";

export const CartCointainer = styled.div`
  * {
    margin-left: auto;
    cursor: pointer;
  }

  img {
    width: 30px;
  }

  .cart-count {
    margin-left: -10px;
    background-color: black;
    color: white;
    padding-left: 2.5px;
    padding-right: 4px;
    border-radius: 10px;
  }
`;
